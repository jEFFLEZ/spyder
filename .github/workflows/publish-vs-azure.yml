name: Publish to Visual Studio Azure

on:
  workflow_dispatch: {}

jobs:
  publish-azure:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Placeholder: implement Azure publish steps here if needed."