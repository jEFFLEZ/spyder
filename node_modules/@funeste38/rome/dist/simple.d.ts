/** Interface pour un workspace indexé */
export interface IndexedWorkspace {
    id: string;
    alias: string;
    name: string;
    path: string;
    type: 'frontend' | 'backend' | 'shared' | 'api' | 'service' | 'other';
    priority: number;
}
/** Classe pour gérer l'indexation simplifiée des workspaces */
export declare class SimpleIndexer {
    private workspaces;
    private indexMap;
    private initialized;
    constructor();
    /** Initialise l'index si nécessaire */
    private ensureInitialized;
    /** Indexe tous les workspaces détectés */
    private indexWorkspaces;
    /** Détecte le type de workspace basé sur le nom/chemin */
    private detectType;
    /** Calcule la priorité pour le tri */
    private getPriority;
    /** Génère un alias court basé sur le type */
    private generateAlias;
    /** Liste tous les workspaces indexés */
    list(): Promise<IndexedWorkspace[]>;
    /** Trouve un workspace par id, alias, ou nom */
    find(identifier: string): Promise<IndexedWorkspace | null>;
    /** Recherche floue par nom */
    search(query: string): Promise<IndexedWorkspace[]>;
}
export declare const indexer: SimpleIndexer;
