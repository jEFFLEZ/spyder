export interface Route {
    method: string;
    path: string;
    name?: string;
}
export type RouteTable = Route[];
export interface Registry {
    findRoute(name: string): Route | undefined;
    url(name: string, params?: Record<string, string | number>): string;
    getRoutes(): RouteTable;
}
/**
 * Creates a route registry from a route table.
 */
export declare function makeRegistry(routes: RouteTable): Registry;
