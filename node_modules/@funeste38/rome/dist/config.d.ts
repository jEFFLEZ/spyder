export interface RomeConfig {
    workspaces?: {
        patterns?: string[];
        exclude?: string[];
    };
    packageManager?: 'npm' | 'yarn' | 'pnpm' | 'auto';
    scripts?: {
        build?: string;
        test?: string;
        lint?: string;
        dev?: string;
    };
    output?: {
        plain?: boolean;
        json?: boolean;
        noEmoji?: boolean;
        ci?: boolean;
    };
}
/** Charge la configuration rome.json depuis la racine du projet */
export declare function loadConfig(root?: string): RomeConfig | null;
/** Options d'affichage résolues */
export interface OutputOptions {
    plain: boolean;
    json: boolean;
    noEmoji: boolean;
    ci: boolean;
}
/** Options d'affichage parsées depuis CLI (undefined = non spécifié) */
export interface ParsedOutputOptions {
    plain?: boolean;
    json?: boolean;
    noEmoji?: boolean;
    ci?: boolean;
}
/** Parse les options d'affichage depuis les arguments CLI */
export declare function parseOutputOptions(args: string[]): ParsedOutputOptions;
/** Fusionne la config utilisateur avec les valeurs par défaut */
export declare function mergeConfig(userConfig: RomeConfig | null): RomeConfig;
/** Fusionne les options CLI avec la config (CLI prioritaire) */
export declare function resolveOutputOptions(cliOptions: ParsedOutputOptions, configOptions?: RomeConfig['output']): OutputOptions;
