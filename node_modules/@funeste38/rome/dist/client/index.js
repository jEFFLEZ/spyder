// src/shared/routes.ts
var routes = [
  { method: "GET", path: "/", name: "home" },
  { method: "GET", path: "/login", name: "login" },
  { method: "POST", path: "/login", name: "login.submit" },
  { method: "GET", path: "/register", name: "register" },
  { method: "POST", path: "/register", name: "register.submit" },
  { method: "GET", path: "/dashboard", name: "dashboard" },
  { method: "GET", path: "/u/:username", name: "profile" },
  { method: "GET", path: "/ranking", name: "ranking" },
  { method: "GET", path: "/salon", name: "salon" },
  { method: "GET", path: "/duel", name: "duel" },
  { method: "GET", path: "/retro", name: "retro" }
  // Add more as needed
];

// src/client/index.ts
function getRoutes() {
  return routes;
}
function findRoute(name) {
  return routes.find((r) => r.name === name);
}
function url(name, params = {}) {
  const r = findRoute(name);
  if (!r) throw new Error(`Route '${name}' not found`);
  return r.path.replace(/:([A-Za-z_]+)/g, (_, k) => encodeURIComponent(String(params[k] ?? `:${k}`)));
}

export { findRoute, getRoutes, url };
//# sourceMappingURL=index.js.map
//# sourceMappingURL=index.js.map