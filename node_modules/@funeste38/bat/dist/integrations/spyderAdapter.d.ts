import { EchoRadar, BatWings } from '../index';
import { Fangs, Channel } from '../fangs';
export interface SpyderBridge {
    askA11Raw: (channel: Channel, text: string) => Promise<any>;
    type?: string;
}
export declare function createSpyderAdapter(bridge: SpyderBridge, fangs?: Fangs): {
    callA11: (text: string) => Promise<any>;
    askA11Multi: (type: string, text: string) => Promise<{
        channel: Channel;
        result: any;
    }>;
    radar: EchoRadar;
    wings: BatWings;
};
