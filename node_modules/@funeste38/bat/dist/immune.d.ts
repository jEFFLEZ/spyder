export declare class Immune {
    private blacklist;
    private penaltyMs;
    isBlacklisted(channelId: string): boolean;
    blacklistChannel(channelId: string, durationMs?: number): void;
    unblacklist(channelId: string): void;
    prune(): void;
    list(): {
        id: string;
        until: number;
    }[];
}
