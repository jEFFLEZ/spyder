"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Memory = void 0;
class Memory {
    constructor(size = 50) {
        this.ring = [];
        this.size = Math.max(1, size);
    }
    push(sig) { this.ring.push(sig); if (this.ring.length > this.size)
        this.ring.shift(); }
    last(n = 10) { return this.ring.slice(Math.max(0, this.ring.length - n)); }
    clear() { this.ring = []; }
    find(predicate) { return this.ring.find(predicate); }
}
exports.Memory = Memory;
