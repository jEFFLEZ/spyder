export type HeartCallback = () => void;
export declare class Heart {
    intervalMs: number;
    private timer;
    private callbacks;
    running: boolean;
    constructor(intervalMs?: number);
    start(): void;
    stop(): void;
    onTick(cb: HeartCallback): void;
    private tick;
    reset(): void;
}
