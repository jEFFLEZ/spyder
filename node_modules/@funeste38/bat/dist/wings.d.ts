import { RequestClass } from './bat';
export declare class Wings {
    state: {
        rate: number;
        retries: number;
        safeMode: boolean;
    };
    consecutiveTimeouts: number;
    readonly maxTimeouts = 3;
    private lastAdjustAt;
    private profile;
    selectProfile(type: string): any;
    applyProfile(echoClass: RequestClass): void;
    computeTimeout(baseline: number): number;
    shouldRetry(attempts: number): boolean;
    shouldAbort(): boolean;
    resetTimeouts(): void;
    reset(): void;
}
