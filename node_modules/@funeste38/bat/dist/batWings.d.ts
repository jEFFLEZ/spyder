import { WingProfile } from './profiles';
export type RequestClass = 'short' | 'normal' | 'slow' | 'timeout';
export declare class BatWings {
    state: {
        rate: number;
        retries: number;
        safeMode: boolean;
    };
    consecutiveTimeouts: number;
    readonly maxTimeouts = 3;
    private lastAdjustAt;
    private profile;
    selectProfile(type: string): WingProfile;
    applyProfile(echoClass: RequestClass): void;
    adjustRate(cls: RequestClass): void;
    computeTimeout(baseline: number): number;
    shouldRetry(attempts: number): boolean;
    shouldAbort(): boolean;
    resetTimeouts(): void;
    drop(id: string): void;
    emergencyRecovery(): Promise<void>;
}
