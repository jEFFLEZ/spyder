export declare class Hormones {
    stress: number;
    private decayMs;
    private lastUpdate;
    inc(amount?: number): number;
    dec(amount?: number): number;
    private updateDecay;
    setStress(v: number): void;
}
