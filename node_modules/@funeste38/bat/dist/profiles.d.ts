export interface WingProfile {
    rateGain: number;
    rateLoss: number;
    maxRetries: number;
    timeoutFactor: number;
    aggressiveness?: number;
}
export declare const defaultProfiles: Record<string, WingProfile>;
