SPYDER Core

Simple MVP that listens on TCP port 4000 and forwards TYPE=1 packets to a local A-11 bridge at http://localhost:5000/ask. Stores messages in RAM/cache and snapshots to dead memory.

Run:

- npm install
- npm run start
